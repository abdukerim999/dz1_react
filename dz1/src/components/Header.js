import React from 'react';
import Title from '../Title';

const Header = () => {
    return (
        <header>
            <Title />
            <h1> Header</h1>
        </header>
    );
};

export default Header;
