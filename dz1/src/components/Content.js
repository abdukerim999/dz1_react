import React from 'react';
import Title from '../Title';

const Content = () => {
    return (
        <main>
            <Title />
            <p> Content</p>
        </main>
    );
};

export default Content;
