import React from 'react';
import Title from '../Title';

const Footer = () => {
    return (
        <footer>
            <Title />
            <p>This is the Footer</p>
        </footer>
    );
};

export default Footer;
